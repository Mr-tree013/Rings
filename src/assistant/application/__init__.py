"""Application layer: use-case orchestration over domain rules and ports.

Contains path resolution (`paths.py`) and, from later phases on, the use cases that
orchestrate domain rules through ports (see docs/specs/0001-system-design.md). No event
processing exists yet.
"""
