"""Domain layer: pure models and rules.

Hard constraints (see AGENTS.md and ADR-0001): this layer must not import adapters,
databases, web frameworks or model SDKs, and must not perform I/O. Phase 0 defines no
domain entities yet — the vocabulary is frozen in the system design spec.
"""

